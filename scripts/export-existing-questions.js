/* Converts the existing original browser question bank to the importer format without losing IDs or answer keys. */
const fs=require('fs'),vm=require('vm'); const source=fs.readFileSync('certification-data.js','utf8'); const sandbox={};vm.createContext(sandbox);vm.runInContext(`${source};this.bank=questionBank`,sandbox);
const records=sandbox.bank.map(q=>({certification:q.certification,external_id:q.id,domain:q.domain,question_text:q.question,question_type:q.type==='single-answer'?'single_answer':q.type,difficulty:q.difficulty,options:q.options.map((text,i)=>({key:String.fromCharCode(65+i),text})),correct_option_keys:[String.fromCharCode(65+q.correctAnswer)],explanation:q.explanation,reference_links:q.referenceLinks||[],active:true,version:1}));
fs.writeFileSync('supabase/existing-questions.seed.json',JSON.stringify(records,null,2)); console.log(`Exported ${records.length} existing community questions to supabase/existing-questions.seed.json.`);
