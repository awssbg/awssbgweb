# Public browser configuration (safe to expose; never add a service-role key here)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Used only by trusted local import scripts; do not place this value in Netlify browser variables.
SUPABASE_SERVICE_ROLE_KEY=
